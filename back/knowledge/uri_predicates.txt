------------------------------------------------------------
GLOBAL URI PREDICATES — B predicates
------------------------------------------------------------

URI_artist(a,x) := hasURI("http://musicbrainz.org/artist/", [a.gid, "#_"], x)

URI_area(ar,x) := hasURI("http://musicbrainz.org/area/", [ar.gid, "#_"], x)

URI_label(l,x) := hasURI("http://musicbrainz.org/label/", [l.gid, "#_"], x)

URI_medium(m,x) := hasURI("http://musicbrainz.org/record/", [m.id, "#_"], x)

URI_place(p,x) := hasURI("http://musicbrainz.org/place/", [p.gid, "#_"], x)

URI_recording(r,x) := hasURI("http://musicbrainz.org/recording/", [r.gid, "#_"], x)

URI_release(r,x) := hasURI("http://musicbrainz.org/release/", [r.gid, "#_"], x)

URI_releaseGroup(rg,x) := hasURI("http://musicbrainz.org/release-group/", [rg.gid, "#_"], x)

URI_signalGroup(rg,x) := hasURI("http://musicbrainz.org/signal-group/", [rg.gid, "#_"], x)

URI_track(t,x) := hasURI("http://musicbrainz.org/track/", [t.id, "#_"], x)

URI_tag(tg,x) := hasURI("http://musicbrainz.org/tag/", [tg.name, "#_"], x)